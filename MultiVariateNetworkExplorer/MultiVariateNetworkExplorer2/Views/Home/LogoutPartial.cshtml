@using System.Security.Claims;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<div class="col-2 d-flex flex-column align-items-end justify-content-center" id="logout_container">
    <span>@("User: " + User.Claims?.FirstOrDefault(x => x.Type.Equals(ClaimTypes.Name, StringComparison.OrdinalIgnoreCase))?.Value)</span>
    <a asp-controller="Login" asp-action="Logout">Logout</a>
</div>
